# ==============================================================================
# Multimarket - System, Runtime, and Dependency Requirements
# ==============================================================================

# [Runtime & Engine Requirements]
Node.js >= 18.0.0
npm >= 9.0.0

# [Database Requirements]
SQLite3 >= 3.x (Bundled internally via better-sqlite3)

# [API / Credentials Requirements]
RapidAPI Key (Required for live product crawling from Amazon, Walmart, Flipkart, Snapdeal)
Internet Connection (Required for external API calls; falls back to mock data offline)

# [Backend Core Dependencies (Node.js/Express)]
express >= 5.2.1
better-sqlite3 >= 12.10.0
cors >= 2.8.6
dotenv >= 17.4.2
concurrently >= 9.2.1

# [Frontend Animation & 3D Dependencies (Vite/React/HTML5)]
vite >= 8.0.12
three >= 0.184.0
postprocessing >= 6.39.1
gsap >= 3.15.0
framer-motion >= 12.40.0
motion >= 12.40.0
lucide-react >= 1.16.0
tailwind-merge >= 3.6.0
class-variance-authority >= 0.7.1
clsx >= 2.1.1
react-countup >= 6.5.3
